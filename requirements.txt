# Core config and API
PyYAML==6.0.2
python-dotenv==1.0.1
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# LangChain primitives (0.3.x series — phải đồng bộ nhau)
langchain-core==0.3.63
langchain-community==0.3.24
langchain-text-splitters==0.3.8

# PDF loader
pypdf==5.1.0
pdfplumber==0.11.4
pandas==2.2.3
tabulate==0.9.0

# Chunking and retrieval helpers
tiktoken==0.8.0
rank-bm25==0.2.2

# Embedding and vector DB
numpy>=1.26.4           # relax pin để tương thích Python 3.11+
google-genai>=1.0.0,<2.0.0  # upper bound tránh breaking changes
cohere>=5.15.0,<6.0.0       # upper bound tránh breaking changes
pinecone>=6.0.0,<7.0.0      # upper bound tránh breaking changes
